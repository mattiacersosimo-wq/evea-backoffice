// ItemHeader no longer needed — the new Item component is self-contained.
// Kept as no-op export so existing Progress files that import it don't break.
const ItemHeader = () => null;
export default ItemHeader;
